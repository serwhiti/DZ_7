# На числовой прямой в точке 0 находится кузнечик. Он умеет прыгать только вперед, делая прыжки от 1 до L делений, т.е. из точки K он может переместиться в точку K+1,
# ..., K+L за один прыжок. Определите, сколько существует различных путей кузнечика, чтобы попасть из точки 0 в точку N. 
# Ввод и вывод организовать при помощи текстовых файлов. В выходной файл нужно вывести единственное число: количество различных путей

input_filename = input("Введите имя файла для чтения: ")
output_filename = input("Введите имя файла для записи: ")
with open(input_filename, "r") as input_file, open(output_filename, "w") as output_file:
    n = int(input_file.readline().strip())
    l = int(input_file.readline().strip())
    a = [0] * (n + 1)
    a[0] = 1

    for i in range(1, n + 1):
        for j in range(1, l + 1):
            if i - j >= 0:
                a[i] += a[i - j]
    output_file.write(str(a[n]))
